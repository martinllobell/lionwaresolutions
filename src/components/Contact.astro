---
import { Icon } from 'astro-icon/components';
---

<section id="contact" class="py-16 px-4 text-gray-800 dark:text-gray-100">
  <div class="text-center mb-16">
    <h1 class="text-3xl lg:text-4xl font-bold tracking-tight">
      Have a project in mind? <span class="text-primary">Contact</span> us!
    </h1>
  </div>

  <!-- Disposición en tres columnas -->
  <div class="grid lg:grid-cols-3 gap-8 max-w-7xl mx-auto items-start">
    <!-- Información de Contacto -->
    <div class="flex flex-col justify-between">
      <div>
        <h2 class="text-2xl font-bold mb-4">Let's Build Something Great Together</h2>
        <p class="mb-6 text-lg">
          Whether you have an idea or need assistance with a project, our team is here to help you achieve your business goals. Connect with us via email or through our social platforms, and let’s start building the future of your business.
        </p>
        <div class="flex flex-col gap-4 text-lg">
          <div class="flex items-center gap-x-4">
            <a href="https://github.com/" target="_blank" class="hover:text-primary flex items-center gap-x-2 transition" aria-label="GitHub Profile">
              <Icon name="github" class="w-6 h-6" />
              GitHub
            </a>
            <a href="https://www.linkedin.com/" target="_blank" class="hover:text-primary flex items-center gap-x-2 transition" aria-label="LinkedIn Profile">
              <Icon name="linkedin" class="w-6 h-6" />
              LinkedIn
            </a>
          </div>
          <a href="mailto:contact@lionware.com" class="text-lg font-bold hover:text-primary flex items-center gap-x-2 transition">
            <Icon name="envelope" class="w-5 h-5" />
            contact@lionware.com
          </a>
        </div>
      </div>
    </div>

    <!-- Formulario de Contacto -->
    <div>
      <form class="flex flex-col space-y-6" method="POST">
        <div>
          <label for="email" class="block mb-2 text-sm font-semibold">Your Email</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="you@example.com"
            class="w-full p-3 rounded-md bg-transparent border border-gray-500 dark:border-gray-700 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-primary"
          />
        </div>
        <div>
          <label for="subject" class="block mb-2 text-sm font-semibold">Subject</label>
          <input
            type="text"
            id="subject"
            name="subject"
            required
            placeholder="Project or Service Inquiry"
            class="w-full p-3 rounded-md bg-transparent border border-gray-500 dark:border-gray-700 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-primary"
          />
        </div>
        <div>
          <label for="message" class="block mb-2 text-sm font-semibold">Message</label>
          <textarea
            id="message"
            name="message"
            placeholder="Tell us about your project or how we can help..."
            required
            class="w-full p-3 rounded-md bg-transparent border border-gray-500 dark:border-gray-700 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-primary"
            rows="5"
          ></textarea>
        </div>
        <button type="submit" class="bg-primary text-white font-semibold py-3 rounded-md hover:bg-primary-dark transition duration-300">
          Get in Touch
        </button>
      </form>
    </div>

    <!-- Calendly Widget -->
    <div class="bg-white dark:bg-slate-800 rounded-md shadow-md p-4">
      <div class="calendly-inline-widget" data-url="https://calendly.com/lionwaresolution/30min" style="min-width:100%; height:450px;"></div>
      <script is:inline type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
    </div>
  </div>
</section>

<style is:global>
  /* Colores principales y transiciones */
  .text-primary {
    color: #58a6ff;
  }

  .bg-primary {
    background-color: #58a6ff;
  }

  .bg-primary-dark {
    background-color: #1f6feb;
  }

  /* Transición suave para el botón */
  button[type="submit"]:hover {
    background-color: #1f6feb;
  }

  /* Transiciones en enlaces */
  a:hover {
    color: #58a6ff;
  }

  /* Ajustes para el widget de Calendly en modo oscuro */
  .calendly-inline-widget iframe {
    background: transparent;
  }
</style>
